{
  "name": "chatbot-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "./mvnw clean package -DskipTests",
    "start": "java -Dspring.profiles.active=prod -Dserver.port=$PORT -jar target/*.jar"
  },
  "engines": {
    "node": "18.x"
  }
}